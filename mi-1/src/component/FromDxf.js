import React, { useState } from "react";
import Image from "./Image/background.png"

const FromDxf = () => {
  return (
    <div className="flex flex-col items-center justify-center min-h-screen p-6 bg-gray-100" style={{display: "flex",width: '100vw', height: '75vh', textAlign: "center",justifyContent: "center", alignItems: "center", color: "green"}}>
      <h3>From dxf will update in future</h3>
    </div>
  );
};

export default FromDxf;
